import * as React from 'react';
import {
    Box, Select, MenuItem,
    CircularProgress, Typography
} from '@mui/material';

export default function DemographicsTab({ organizationID }) {

    return(
        <Typography>This is where demographics graphs would go... IF I HAD ANY</Typography>
    );
}